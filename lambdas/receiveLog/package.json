{
  "name": "receive-log",
  "version": "1.0.0",
  "private": true,
  "description": "Receives sudo log from device on expiration and uploads it as a .txt attachment to the Slack thread.",
  "main": "index.js",
  "dependencies": {
    "@aws-sdk/client-dynamodb": "^3.600.0",
    "@aws-sdk/lib-dynamodb": "^3.600.0"
  }
}
